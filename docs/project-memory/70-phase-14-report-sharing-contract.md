# Phase 14 Report Sharing Contract

Share links use cryptographically random tokens. Only token hashes are stored. Share links are read-only, inherit report expiry, and may be revoked. Owner access and public share access are separate.
